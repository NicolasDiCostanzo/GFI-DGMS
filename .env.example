# Overrides the URL the widget fetches grant data from at runtime.
# Defaults to a jsdelivr URL tracking this repo's main branch.
# VITE_GRANT_DATA_URL=https://cdn.jsdelivr.net/gh/<owner>/<repo>@<ref>/src/sovereign/infrastructure/data/grants.json
